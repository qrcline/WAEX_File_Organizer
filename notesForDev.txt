OutputFolder = QFileDialog::getExistingDirectory(0, ("Select Output Folder"), QDir::currentPath());

void FolderIo::copyFile(QString source, QString destination, QString destinationName)
{
    QFile::copy(source, destination+"/"+destinationName);//This copies the file with the new destination name


}



//Used to list out the folders in a directory calling func
  std::vector<std::string> path=get_directories("D:/Test");
       for (auto i = path.begin(); i != path.end(); ++i)
           std::cout << *i << ' '<<std::endl;

D:\Test\1067 D:/Test/999